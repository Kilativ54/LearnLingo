import React from 'react';
import { LogoText } from './Logo.styled';



const Logo = ({ size }) => {
  return (
    
       <LogoText to="/"><p>LearnLingo</p></LogoText>

    
  );
};

export default Logo;
